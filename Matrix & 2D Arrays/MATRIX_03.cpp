#include <bits/stdc++.h>
#define l long long
#define vl vector<long long> 
#define vi vector<int> 
#define v2 vector<vector<int>> 
#define IOS ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0) 
using namespace std ;

// passing 2d array as argument in C++
// what is a jagged array
// a 2d array where size of each array is different is k/a jagged array or 2d array where we have different number of elements in different rows. for example when we have an array of vectors where we can dynamically change the size of each row

int main()
{
    IOS;
    // well it is simple, so I did not write the code

    return 0;
}